#include <stdio.h>
#include <stdlib.h>
#include <SDL2/SDL.h>
#include "movement.h"

// Move a posição para cima (diminui y)
void mover_para_cima(Posicao* pos) {
    return;
}

// Move a posição para baixo (aumenta y)
void mover_para_baixo(Posicao* pos) {
    return;
}

// Move a posição para a esquerda (diminui x)
void mover_para_esquerda(Posicao* pos) {
    return;
}

// Move a posição para a direita (aumenta x)
void mover_para_direita(Posicao* pos) {
    return;
}
